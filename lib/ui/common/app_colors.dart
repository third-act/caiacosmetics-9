import 'package:flutter/material.dart';

/// Soft depth tokens — cream canvas, blush accent, ink type.
class AppColors {
  AppColors._();

  static const accent = Color(0xFFE0CCC7); // blush
  static const accentSoft = Color(0x33E0CCC7);
  static const accentStrong = Color(0xFF6F6464);
  static const onAccent = Color(0xFF333333);

  static const money = Color(0xFF6F6464);
  static const danger = Color(0xFFB3261E);

  static const bg = Color(0xFFFFFCF7); // cream
  static const surface = Color(0xFFFFFFFF);
  static const surfaceRaised = Color(0xFFFFFFFF);
  static const hairline = Color(0x14333333);
  static const ink = Color(0xFF333333);
  static const inkMuted = Color(0xFF6F6464);
  static const inkFaint = Color(0xFF9A9191);

  static const shadow = Color(0x14333333);

  static const plateHigh = Color(0xFFFFF8F3);
  static const plateLow = Color(0xFFF0E4DE);
  static const skeletonBase = Color(0xFFEDE6E0);
  static const skeletonHighlight = Color(0xFFF8F4F0);
}
